import { redirect } from "next/navigation";
/** The New Hampshire Sandbox graduated into the main map engine (components/map/MarineMap.tsx) on 2026-09-22. */
export default function NhSandbox() { redirect("/"); }
